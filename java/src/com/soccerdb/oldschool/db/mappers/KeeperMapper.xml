<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.soccerdb.oldschool.db.dao.KeeperDAO">
 	<resultMap id="result" type="Keeper">
 		<!-- Instance of Entity and Attributes of Relation need to be delclared HERE! -->
        <result property="player_id" column="player_id"/>
		<result property="player_total_save" column="total_save"/>
		
 	</resultMap>
 	
 	<select id="selectAll" resultMap="result">
 		<!--SELECT * FROM "Relation";-->
		 SELECT * FROM Keepers;
 	</select>
 	
 	<select id="selectById" parameterType="int" resultMap="result">
 		<!--SELECT * FROM "Relation" WHERE ???;-->
		 SELECT * FROM Keepers WHERE player_id = #{player_id};
 	</select>
 	
 	<insert id="insert" parameterType="Keeper" useGeneratedKeys="true" keyProperty="id">
 		<!-- insert into "Relation"  ("attribute name of relation" , ... , ???)
 		values "instace name of entity" , ... , "instace name of entity" -->
		INSERT INTO Keepers (player_id, total_save)
		VALUES (#{player_id}, #{player_total_save});

 	</insert>
 	
 	<update id="update" parameterType="Keeper">
 		UPDATE Keepers
 		SET total_save = #{player_total_save}
 		WHERE player_id = #{player_id};
 	</update>
 	
 	<delete id="delete" parameterType="int">
 		DELETE FROM Keepers WHERE player_id = #{player_id};
 	</delete>
 </mapper>